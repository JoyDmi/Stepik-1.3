/*Разность времен

Даны значения двух моментов времени, принадлежащих одним и тем же суткам: часы, минуты и секунды для каждого из моментов времени. Известно, что второй момент времени наступил не раньше первого. Определите, сколько секунд прошло между двумя моментами времени.

Формат входных данных

Программа на вход получает три целых числа: часы, минуты, секунды, задающие первый момент времени и три целых числа, задающих второй момент времени.

Формат выходных данных

Выведите число секунд между этими моментами времени.*/
#include <iostream>
using namespace std;
int main() {
    int h1, h2, m1, m2, s1, s2, time1, time2;
    cin >> h1 >> m1 >> s1 >> h2 >> m2 >> s2;
    time1 = (h1 * 3600) + (m1 * 60) + s1;// всего секунд в первом моменте времени
    time2 = (h2 * 3600) + (m2 * 60) + s2;// всего секунд во втором моменте времени
    cout << (time2 - time1); // разность времени  или промежуток времени от 1 до 2
    return 0;
}